name: Docker Image CI
on:
  push: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - id: cache-docker
      uses: actions/cache@v1
      with:
        path: /var/lib/docker
        key: var-lib-docker-${{ hashFiles('Dockerfile') }}
    - run: docker build . -t thing
